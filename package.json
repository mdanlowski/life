{
  "name": "life",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "npx browser-sync start -s -f . --no-notify --host 127.0.0.1 --port 9000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "p5": "^1.3.1"
  }
}
